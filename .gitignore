# binary
go_cache

# persistence data (per-mode dirs + generic names)
data/
data-*/
dump.ccs
appendonly.aof
*.ccs
*.ccs.tmp
*.aof
*.aof.tmp

# local TLS material (see config.security.example.json)
certs/
