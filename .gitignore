# binary
go_cache

# persistence data (per-mode dirs + generic names)
data/
data-*/
dump.ccs
appendonly.aof
*.ccs
*.ccs.tmp
*.aof
*.aof.tmp

# local TLS material (see config.security.example.json)
certs/

# deploy env (copy from deploy/.env.example)
deploy/.env
.env

# local bench binary + raw result dumps (docs summarize published baselines)
bench/.bin/
bench/results/
bench/configs/*.generated.json

# scratch M11 redis-cli smoke scripts (review before committing)
scripts/m11-*.sh
